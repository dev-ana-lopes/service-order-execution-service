apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-order-execution-service-worker
  namespace: service-order
  labels:
    app: service-order-execution-service-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-order-execution-service-worker
  template:
    metadata:
      labels:
        app: service-order-execution-service-worker
    spec:
      containers:
        - name: worker
          image: __API_IMAGE__
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.worker"]
          envFrom:
            - configMapRef:
                name: service-order-execution-service-config
            - secretRef:
                name: service-order-execution-service-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
