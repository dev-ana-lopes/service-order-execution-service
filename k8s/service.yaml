apiVersion: v1
kind: Service
metadata:
  name: service-order-execution-service
  namespace: service-order
  labels:
    app: service-order-execution-service
spec:
  type: ClusterIP
  selector:
    app: service-order-execution-service
  ports:
    - name: http
      port: 80
      targetPort: 8000

